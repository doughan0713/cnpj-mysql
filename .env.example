# Database Connection Settings
# Supported types: mysql, postgres (or postgresql)
DB_TYPE=mysql

# Database Server Credentials & Address
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=cnpj
DB_USER=root
# Leave blank if no password is required, or specify your password securely.
# Do not commit secrets/passwords to git repository.
DB_PASS=
